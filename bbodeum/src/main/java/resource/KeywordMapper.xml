<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bbodeum.Keyword.dao.KeywordMapper">
<resultMap id="KeywordMap" type="keyword" autoMapping="true">
</resultMap>

  <select id="insert" parameterType="map" >
	insert into keyword values(key_id.nextval, #{keyName})


	</select>
</mapper>